# Oracle weekday routine — full prompt (self-merge policy approved by the owner on 2026-09-24)

Paste everything below the line into the scheduled task *FNAM Oracle: weekday review* (Claude desktop app →
Routines → the task → edit prompt), replacing the current prompt. It is the current prompt plus the merge
conditions; the sandbox does not let a Claude session install a self-merge instruction on its own.

---

This is a fully autonomous weekday check for the Oracle financial model at https://fnam.mx/oracle (GitHub repository marthavshelton-sys/fnam-debt-monitor, folders site/oracle + tools/oracle + scripts/oracle; runbook in tools/oracle/README.md, data contracts and conventions in tools/oracle/ASSUMPTIONS.md, merge policy in tools/oracle/ROUTINE.md). No user is present — do not ask questions; make reasonable judgment calls per the rules below and proceed. The owner has approved a self-merge policy (2026-09-24) so the page updates with no action on their part: every repository change goes through a pull request that YOU merge yourself (`gh pr merge <n> --squash --delete-branch --repo marthavshelton-sys/fnam-debt-monitor`) as soon as the MERGE CONDITION stated in the step is met; leave a PR open only where a step says so. Use public information only (SEC filings, Oracle's investor site and press releases, official market data, the developers' own releases and major wire reports for data-center sites); never seek or use non-public information; never invent a number — leave a field null or "not disclosed" rather than estimate, and mark any figure computed from ratios management gave with `derived: true`. Git is at "C:\Program Files\Git\cmd\git.exe" (also on PATH in Git Bash); pushes authenticate through Git Credential Manager; the GitHub CLI (`gh`, at "C:\Program Files\GitHub CLI\gh.exe") is logged in — use it for pull requests (`gh pr create`, `gh pr list`, `gh pr merge`). Never use bare `git stash`.

STEP 1 — Sync. Work ONLY in the routine's own clone C:\Users\MARTH\OneDrive\Desktop\Talipot\fnam-oracle-routine (never in C:\Users\MARTH\OneDrive\Desktop\Talipot\fnam-debt-monitor or fnam-oracle-wt, which other sessions and the other pages' work use — touching them would interfere with their workflows; if a file edit there is refused for permissions, write the file with a node one-liner instead of stopping). Run `git fetch --prune origin`, `git checkout main`, `git pull --ff-only`. Read tools/oracle/notify-state.json. `gh pr list --repo marthavshelton-sys/fnam-debt-monitor`: if a PR from branch `oracle/notify-state` is still open, reuse that branch (rebase it onto origin/main, push with `--force-with-lease`) instead of opening another.

STEP 2 — Harvest. Run `node scripts/oracle/harvest-filings.mjs` (SEC EDGAR is reachable from this machine even when it refuses GitHub's runners; the script falls back to Oracle's IR JSON feed). Then read tools/oracle/data/state.json. If pending_extraction contains an 8-K or an "IR release" entry with status "pending", a new quarter has landed → STEP 3. If it contains a new 10-Q or 10-K with status "pending" → STEP 4. Otherwise → STEP 5.

STEP 3 — New quarter. Open the archived exhibit named in pending_extraction[].archived (for an "IR release" entry, run the harvester again or download the 8-K EX-99.1 from EDGAR, CIK 0001341439, and archive it under tools/oracle/raw/8k/ with the same naming as the existing files). Following tools/oracle/README.md ("Adding a quarter by hand") and the schema of the latest record in tools/oracle/data/quarters.json, write tools/oracle/data/_raw_fy20xx.json in the shape of the existing _raw_fy2026.json: GAAP statement, Non-GAAP reconciliation, balance-sheet highlights, cash flow (Q2–Q4 cash flows are printed cumulatively in Oracle's release and must be made discrete by subtracting the previous cumulative release), RPO, dividend declared, guidance issued, D&A. Run `node scripts/oracle/merge-raw.mjs`. Add the new guidance vintage to tools/oracle/data/guidance.json in the shape of the latest one; every free-text field must be bilingual: `_note` with `_note_es`, `fy_capex_note` with `fy_capex_note_es`, `multi_year_targets.note` with `multi_year_targets.note_es` (Spanish is the page's default language). Draft the Comments column and the executive-summary bullets for the quarter as tools/oracle/data/_raw_comments_c.json in the shape of _raw_comments_b.json, then run `node scripts/oracle/merge-comments.mjs`. Comment style (the owner's rule): bilingual ES/EN, driver-only, one clause of about 12–15 words naming the cause of the year-over-year change (never restate the percentage the table already shows), "no driver given by management" where the release says nothing, and a `src` citing the release page or the call page and speaker in the concise form "1Q27 call p.6". Cover every key the existing quarters carry: the income-statement lines, the balance-sheet keys (cash, total_assets, deferred_revenue, total_debt, equity), the cash-flow and operating-metrics keys (operating_cash_flow, capex, free_cash_flow, da, ebitda_margin, dividend, diluted_shares) and rpo/cloud. When the earnings-call transcript is available (the owner drops the PDF into the private repository marthavshelton-sys/oracle-model, folder Transcripts, cloned at C:\Users\MARTH\OneDrive\Desktop\Talipot\Oracle; short attributed quotes only), also update tools/oracle/data/buildout.json following its existing entries: (a) the quarter's megawatts delivered (capacity.quarters), GPU utilization, renewals and GPUs delivered (gpu.*), capacity secured; (b) any new or updated data-center site (sites[]: capacity, customer, developer, financing, Oracle's status, contracted, first delivery — each free-text field in English plus its `_es` counterpart — and sources with a `short` label such as "1Q27 call p.6" or "Oracle PR 28 Jul 2026" plus page and speaker or URL) — Oracle's own statements first, the developer's release or a major wire report only for what Oracle has not disclosed, "not disclosed by Oracle" / "no divulgado por Oracle" otherwise; (c) `promises`: what management said it will deliver, newest call first, set `promises.as_of` to the new quarter, add `outcome_en/es` to any earlier promise the new call resolved; mark computed figures `derived: true`. If the transcript is not yet available, leave those blocks as they are (the page flags them as pending) and ask for the PDF in the email. Mark the filing status "done" in state.json (fields extracted_into, done_on). Run `node scripts/oracle/build.mjs`; the tie-out and the parser tests must both report 0 failed — if not, fix the transcription (a failure is almost always a typo, not an Oracle error) and re-run. Create a branch named oracle/<fy>q<n>-results, commit tools/oracle/data, tools/oracle/raw and site/oracle/data with a message ending in "Co-Authored-By: Claude <noreply@anthropic.com>", push it, and open a pull request titled "oracle: <nQyy> results" whose body lists the headline figures (revenue, cloud revenue, RPO, GAAP and Non-GAAP EPS, operating cash flow, capex, megawatts delivered if stated) with the exhibit URL and states whether both guards passed. MERGE CONDITION: both guards reported 0 failed (the parser tests re-read the archived exhibit and independently confirm 26 printed figures per quarter; that machine check is the review) → merge it (squash, delete branch) and `git pull --ff-only` main. If either guard still fails after your fixes, leave the PR open and say so in the email.

STEP 4 — New 10-Q / 10-K. Read the filing on EDGAR. From a 10-K: update tools/oracle/data/fiscal_years.json (annual statement including gaap.revenue lines by type, D&A, cash flow) and the debt footnote into market_reference.json → debt_instruments (every note with principal, coupon, issue and maturity dates; the principal must reconcile to the disclosed gross total) and the shares outstanding on the cover. From a 10-Q: update the shares outstanding on the cover (market_reference.json → price_snapshot.orcl.shares_outstanding_millions, dated) and retire any note repaid in the quarter. Mark the filing done, run the build, open a PR as in STEP 3 (branch oracle/<form>-<period>). MERGE CONDITION: both guards 0 failed and every figure taken from the filing itself → merge.

STEP 5 — Events. Fetch Oracle's press-release list with curl.exe (not the browser) from the Q4 JSON endpoint with the FULL parameter set: `https://investor.oracle.com/feed/PressRelease.svc/GetPressReleaseList?LanguageId=1&bodyType=0&pressReleaseDateFilter=3&categoryId=1cb807d2-208f-4bc3-9133-6a9ad45ac3b0&pageSize=20&pageNumber=0&tagList=&includeTags=true&year=<current year>&excludeSelection=1` (the short query without categoryId returns an empty list since 2026-09-24 and the RSS view answers HTTP 403; if the JSON endpoint fails, fall back to https://www.oracle.com/news/). Also list Oracle's recent EDGAR submissions (https://data.sec.gov/submissions/CIK0001341439.json, with a descriptive User-Agent) for non-earnings 8-Ks, FWP or 424B prospectuses newer than lastEventDate — the harvester only archives earnings 8-Ks, 10-Qs and 10-Ks. Look at items newer than notify-state.lastEventDate: debt issuances or redemptions, dividend changes, rating actions (also check the Moody's, S&P and Fitch Oracle pages if a release mentions ratings), buybacks, major contracts, capex announcements, and data-center site announcements — record those in buildout.json sites[] with the source URL, a short label and bilingual text fields as in STEP 3. Record any other fact with its source URL in market_reference.json (credit_ratings, debt_instruments) or special_situations.json (AI-buildout timeline). Investor calendar: run `node scripts/oracle/fetch-calendar.mjs` (Oracle IR events list and date-setting releases → tools/oracle/data/calendar.json); an event with status "confirmed" whose first_seen is newer than notify-state.lastEventDate is a newly announced date and a material item — report its date, time and webcast link in the email; never edit the generated `events` or `estimates`, add to `manual_events` only a date named on a call or in a release that the IR page does not list yet, with its source. Run the build, and open a PR (branch oracle/events-<date>) if anything changed; in the PR body, mark each fact as primary-sourced (SEC filing, Oracle IR release, the agency's own rating release, the developer's or partner's own release) or press-sourced. MERGE CONDITION: the build passed AND every new fact is primary-sourced → merge. If any new fact rests only on press reports, leave the PR open, list it in the email as "awaiting your review", and note it in tools/oracle/PENDING.md.

STEP 6 — Materiality. Read tools/oracle/data/alerts.json. Today is material if any of: a new quarter or a 10-Q/10-K was filed; a guidance range changed (compare guidance.json vintages against notify-state.lastGuidanceIssuedIn); a rating action; a debt issuance or redemption; a new data-center site or a change in a site's capacity, customer or financing; a newly confirmed investor event in calendar.json; the last ORCL close in tools/oracle/data/prices_orcl_daily.csv moved more than share_move_pct_1d percent versus the previous close; net debt / LTM EBITDA (from site/oracle/data/financials.js, latest four quarters) exceeds net_debt_to_ltm_ebitda_max; or this run opened a pull request.

STEP 7 — Email only on a material day, using the Gmail connector tool mcp__1c227e1e-677a-4723-bf62-d01757e2c8eb__send_message: to ["marthavshelton@gmail.com"]; subject "Oracle model: " + a headline under eight words (for example "1Q27 results: revenue +30%, RPO $664bn"); plain-text body under 150 words: one line per material item with old → new values, dates and the source link; two or three sentences of analysis (why it matters against the guidance and the leverage trend already on the page; what to watch next); for each PR either "merged and live" with its link (you can revert any commit) or "left open, awaiting your review" with the reason; if a new quarter landed and no transcript was available, one sentence asking for the earnings-call transcript PDF (it enriches the Comments column, the management quotes, the guidance record and the buildout data); the line "Full detail: https://fnam.mx/oracle"; and the closing line "Sent automatically by the Oracle model routine. Co-Authored-By: Claude <noreply@anthropic.com>". Combine several material items into one email. On a quiet day send nothing.

STEP 8 — Always: update tools/oracle/notify-state.json (lastQuarter, lastFilingSeen, lastGuidanceIssuedIn, lastEventDate, lastMarketClose, lastNotifiedAt, lastFailureNote, checkedAt) — inside the PR when one was opened this run, otherwise on the reused `oracle/notify-state` branch (see STEP 1) as a small PR only if a value other than checkedAt changed and none is open, and MERGE that state PR immediately (squash, delete branch): it only records the routine's state. If the harvest, the build or a fetch fails, do not email about it; write the error into lastFailureNote (locally, in the working tree) and stop, so the next run retries. Finish with a one-line summary of what happened (for example "No material change in Oracle data today").
